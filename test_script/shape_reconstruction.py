#!/bin/python3

import numpy as np

#### Shape Reconstruction ####

def compute_selected_vertices_cones(directory,mesh,rate_vals,length,threshold=-1,cone_size,ball=True,ball_radius,radius=1)


def summarize_vertices(directory,mesh,rate_vals,length,reduction_operation=np.intersect1d,threshold,cone_size,ball = TRUE, ball_radius = 1, radius = 0):
    picked_indices = rate_vals > threshold
    

    return


#### Heatmap Code ####

def reconstruct_vertices_on_shape(directory,mesh,rate_vals,length,cuts=10,cone_size,ball_radius,ball=True,radius=0):
    
    return


